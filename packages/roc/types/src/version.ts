/**
 * Version metadata (REF-1900.004 requires the type package to export it).
 */

/** WIL protocol version (WIL-001.001, WIL-001.009). */
export const WIL_PROTOCOL_VERSION = "1.0.0";

/** The WIL protocol identifier. Every message's protocol field MUST equal this. */
export const WIL_PROTOCOL = "wil";

/** Reference implementation version. */
export const ROC_REFERENCE_VERSION = "0.1.0";
